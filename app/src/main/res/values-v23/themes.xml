<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.AirlineExplorer" parent="Theme.MaterialComponents.Light">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryVariant">@color/primaryDarkColor</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondaryColor</item>
        <item name="colorSecondaryVariant">@color/secondaryDarkColor</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>

        <!-- Set the status bar as "light" so that its contents (system clock & indicators)
        are displayed with a dark color.
        The "android:windowLightStatusBar" requires API level 23 or higher -->
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>