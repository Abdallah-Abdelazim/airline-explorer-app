<resources>
    <!-- Base application theme. -->
    <style name="Theme.AirlineExplorer" parent="Theme.MaterialComponents.Light">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryVariant">@color/primaryDarkColor</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondaryColor</item>
        <item name="colorSecondaryVariant">@color/secondaryDarkColor</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
    </style>

    <style name="Theme.AirlineExplorer.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Launcher" parent="Theme.AirlineExplorer.NoActionBar">
        <item name="android:windowBackground">@drawable/splash</item>
        <item name="android:statusBarColor">?attr/colorSecondary</item>
    </style>

    <style name="Theme.AirlineExplorer.AppBarOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Theme.AirlineExplorer.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>